interface:
  display_name: "Image to Editable PPT"
  short_description: "Rebuild slide images as faithful editable PPTX pages"
  default_prompt: "Use $image-to-editable-ppt to rebuild the supplied slide image with source-first inventory, native editable objects, declared asset boundaries, and rendered fidelity checks."
policy:
  allow_implicit_invocation: true
