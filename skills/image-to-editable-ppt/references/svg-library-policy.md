# SVG library policy

Search the library before opening an external icon website. `generated-basic` entries are locally authored geometric primitives. External entries must retain source URL, source label, hash, extraction method, and license status.

Never overwrite a library asset with a different hash. A visible page SVG extracted after a blocked download must be described as `inline-rendered-svg` and `fallback-inline`, not as an official download.
